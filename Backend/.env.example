PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=pd_boletera_db
DB_DIALECT=mysql

# JWT Configuration
JWT_SECRET=super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=24h

# Security & CORS
CORS_ORIGIN=http://localhost:4200

# Initial Admin Configuration (Used for seeding)
ADMIN_EMAIL=admin@pdboletera.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Super Admin
